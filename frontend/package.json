{
    "private": true,
    "scripts": {
        "build": "nuxt build",
        "dev": "nuxt dev --port=4000",
        "generate": "nuxt generate",
        "preview": "nuxt preview",
        "postinstall": "nuxt prepare"
    },
    "devDependencies": {
        "nuxt": "^3.2.0"
    }
}
